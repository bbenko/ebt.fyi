{
  "name": "ebt-fyi",
  "version": "1.0.0",
  "private": true,
  "description": "East Bay Tech, rebuilt as a simple Eleventy static site.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site .cache"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
